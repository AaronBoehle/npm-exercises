/*
Create a directory named src. This is where your (untranspiled) javascript code will live.
    Inside of the src directory, create a file named index.js. This is the entry point of your application.
    Inside of index.js, write a function named sayHello using a arrow function, that logs hello to the console. Then call that function.
*/
"use strict";

const sayHello = logHello =>console.log("Hello to the console!");
sayHello();
